# Personal project: always use the public registry. The machine-level ~/.npmrc
# points at Hopper's Artifact Registry, which 401s here and breaks the agent
# runtime's `npm ci` and the development-request worktree installs.
registry=https://registry.npmjs.org/
