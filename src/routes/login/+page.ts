// Sign-in is a server-rendered link and must work even when browser JavaScript
// cannot start (for example, a cached client bundle from an older deployment).
export const ssr = true;
export const csr = false;
